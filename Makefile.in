# sample makefile
CC=cc

targets: default

# this target creates a bin-directory
bindir:
	mkdir -p bin/@GAPARCH@

default: bindir src/hello.c
	$(CC) -o bin/@GAPARCH@/hello src/hello.c

clean:
	rm -rf bin Makefile
